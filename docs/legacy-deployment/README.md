# Archived upstream deployment workflows

These files preserve the original AWS Lambda, Fly, Vercel and upstream npm publishing jobs for reference. GitHub does not run workflows in this directory. The active fork pipeline is `.github/workflows/cloudflare-staging.yml`; CI validates the Cloudflare package and container build.
